<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Alexia en route — Carnets de voyage</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,500;0,9..144,600;1,9..144,400;1,9..144,500&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#1c2430;
    --paper:#f6f3ec;
    --card:#ffffff;
    --navy:#1d3557;
    --red:#c1272d;
    --gold:#b8860b;
    --line:#e3ddcf;
    --muted:#6b6455;
    --serif:'Fraunces', Georgia, serif;
    --sans:'Inter', 'Helvetica Neue', Arial, sans-serif;
  }
  *{box-sizing:border-box;}
  body{
    margin:0;
    background:var(--paper);
    color:var(--ink);
    font-family:var(--sans);
    line-height:1.5;
  }

  /* ---------- Hero ---------- */
  header.hero{
    background:linear-gradient(160deg, var(--navy) 0%, #0f1e33 100%);
    color:#f2efe4;
    padding:80px 24px 64px;
    position:relative;
    overflow:hidden;
    text-align:center;
  }
  header.hero::after{
    content:"";
    position:absolute;
    right:-80px; top:-80px;
    width:320px; height:320px;
    border:2px solid rgba(242,239,228,0.10);
    border-radius:50%;
  }
  header.hero::before{
    content:"";
    position:absolute;
    left:-100px; bottom:-120px;
    width:280px; height:280px;
    border:2px solid rgba(242,239,228,0.08);
    border-radius:50%;
  }
  .eyebrow{
    font-family:var(--sans);
    text-transform:uppercase;
    letter-spacing:0.22em;
    font-size:0.72rem;
    color:#d7c9a0;
    margin-bottom:18px;
    position:relative; z-index:2;
  }
  h1.wordmark{
    font-family:var(--serif);
    font-style:italic;
    font-weight:500;
    font-size:clamp(2.6rem, 7vw, 4.6rem);
    margin:0 0 16px;
    letter-spacing:-0.01em;
    line-height:1.05;
    position:relative; z-index:2;
  }
  .hero-sub{
    font-family:var(--sans);
    color:#c9d2de;
    font-size:1.05rem;
    max-width:480px;
    margin:0 auto;
    position:relative; z-index:2;
  }

  /* ---------- Route line (élément signature) ---------- */
  main{
    max-width:760px;
    margin:0 auto;
    padding:56px 24px 100px;
    position:relative;
  }
  .route-line{
    position:absolute;
    left:38px; top:8px; bottom:60px;
    width:1px;
    background-image:linear-gradient(var(--line) 60%, transparent 0%);
    background-size:1px 8px;
    background-repeat:repeat-y;
  }
  @media (max-width:640px){ .route-line{display:none;} }

  /* ---------- Sections ---------- */
  .section{ margin-bottom:64px; position:relative; }
  .section-head{
    display:flex;
    align-items:baseline;
    gap:14px;
    margin-bottom:28px;
    padding-left:64px;
  }
  @media (max-width:640px){ .section-head{padding-left:0;} }
  .section-eyebrow{
    font-family:var(--sans);
    text-transform:uppercase;
    letter-spacing:0.16em;
    font-size:0.72rem;
    font-weight:700;
    color:var(--red);
  }
  .section-count{
    font-family:var(--serif);
    font-style:italic;
    color:var(--muted);
    font-size:0.95rem;
  }

  /* ---------- Trip card ---------- */
  .trip{
    position:relative;
    padding-left:64px;
    margin-bottom:22px;
  }
  @media (max-width:640px){ .trip{padding-left:0;} }
  .trip-dot{
    position:absolute;
    left:32px; top:28px;
    width:13px; height:13px;
    border-radius:50%;
    background:var(--paper);
    border:2px solid var(--navy);
    z-index:1;
  }
  .trip.past .trip-dot{ border-color:var(--muted); background:var(--line); }
  @media (max-width:640px){ .trip-dot{display:none;} }

  a.trip-card{
    display:block;
    background:var(--card);
    border:1px solid var(--line);
    border-left:4px solid var(--navy);
    border-radius:10px;
    padding:26px 28px;
    text-decoration:none;
    color:var(--ink);
    transition:transform 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease;
  }
  a.trip-card:hover{
    transform:translateY(-2px);
    box-shadow:0 10px 26px rgba(29,53,87,0.14);
  }
  .trip.past a.trip-card{ border-left-color:var(--muted); }

  .trip-top{
    display:flex;
    justify-content:space-between;
    align-items:flex-start;
    gap:16px;
    flex-wrap:wrap;
    margin-bottom:10px;
  }
  .trip-name{
    font-family:var(--serif);
    font-style:italic;
    font-weight:500;
    font-size:1.7rem;
    letter-spacing:-0.01em;
    margin:0;
  }
  .trip-dates{
    font-family:var(--sans);
    font-size:0.72rem;
    text-transform:uppercase;
    letter-spacing:0.08em;
    color:var(--muted);
    white-space:nowrap;
    padding-top:6px;
  }
  .trip-blurb{
    font-family:var(--sans);
    font-size:0.92rem;
    color:var(--muted);
    margin:0 0 16px;
    max-width:480px;
  }
  .trip-cta{
    font-family:var(--sans);
    font-size:0.8rem;
    font-weight:600;
    color:var(--navy);
    display:inline-flex;
    align-items:center;
    gap:6px;
  }
  .trip.past .trip-cta{ color:var(--muted); }
  a.trip-card:hover .trip-cta{ gap:10px; }
  .trip-cta .arrow{ transition:transform 0.15s ease; }

  .trip-tags{
    display:flex; gap:8px; flex-wrap:wrap; margin-bottom:14px;
  }
  .trip-tag{
    font-family:var(--sans);
    font-size:0.66rem;
    text-transform:uppercase;
    letter-spacing:0.06em;
    color:var(--navy);
    background:#eef1f6;
    padding:3px 9px;
    border-radius:20px;
  }
  .trip.past .trip-tag{ color:var(--muted); background:#eeece4; }

  footer{
    text-align:center;
    font-family:var(--sans);
    font-size:0.72rem;
    color:var(--muted);
    padding:0 24px 48px;
  }
</style>
</head>
<body>

<header class="hero">
  <div class="eyebrow">Carnets de voyage</div>
  <h1 class="wordmark">Alexia en route</h1>
  <p class="hero-sub">Un itinéraire par escapade — adresses, horaires et repères, prêts à consulter sur place.</p>
</header>

<main>
  <div class="route-line"></div>

  <section class="section">
    <div class="section-head">
      <span class="section-eyebrow">Upcoming trips</span>
      <span class="section-count">— à venir</span>
    </div>

    <div class="trip">
      <div class="trip-dot"></div>
      <a class="trip-card" href="london/index.html">
        <div class="trip-top">
          <h2 class="trip-name">Londres</h2>
          <span class="trip-dates">9 – 13 septembre 2026</span>
        </div>
        <div class="trip-tags">
          <span class="trip-tag">Tate Modern</span>
          <span class="trip-tag">Camden</span>
          <span class="trip-tag">West End</span>
        </div>
        <p class="trip-blurb">Cinq jours entre Shoreditch, les deux Tate et Notting Hill, avec Chantal et Ricardo.</p>
        <span class="trip-cta">Voir le guide <span class="arrow">→</span></span>
      </a>
    </div>
  </section>

  <section class="section">
    <div class="section-head">
      <span class="section-eyebrow">Past trips</span>
      <span class="section-count">— déjà vécus</span>
    </div>

    <div class="trip past">
      <div class="trip-dot"></div>
      <a class="trip-card" href="usa-2026/index.html">
        <div class="trip-top">
          <h2 class="trip-name">Barcelona → NYC → Chicago → Seattle</h2>
          <span class="trip-dates">Été 2026</span>
        </div>
        <div class="trip-tags">
          <span class="trip-tag">Chelsea</span>
          <span class="trip-tag">Wicker Park</span>
          <span class="trip-tag">Pike Place</span>
        </div>
        <p class="trip-blurb">Quatre villes, un été — de la Méditerranée à Manhattan, des Grands Lacs jusqu'au Pacifique.</p>
        <span class="trip-cta">Voir le guide <span class="arrow">→</span></span>
      </a>
    </div>
  </section>
</main>

<footer>Carnets personnels — mis à jour au fil des voyages</footer>

</body>
</html>
